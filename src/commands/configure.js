module.exports = {
    name: 'configure',
    run: async toolbox => {
        const { print, parameters } = toolbox;

        let type = parameters.first;

        if(!type) {
            
        }
    }
}